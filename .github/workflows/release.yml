name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write   # needed to create GitHub releases and upload assets

jobs:
  build-and-release:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Parse version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Install XcodeGen
        run: brew install xcodegen --quiet

      - name: Generate Xcode Project
        run: xcodegen generate

      # ── Detect certificate availability ─────────────────────────────────────
      # Required secrets for full signing + notarization:
      #   APPLE_CERTIFICATE          — base64-encoded .p12 (Developer ID Application)
      #   APPLE_CERTIFICATE_PASSWORD — .p12 export password
      #   APPLE_TEAM_ID              — 10-character Apple Team ID
      #   APPLE_ID                   — Apple ID email (for notarytool)
      #   APPLE_APP_PASSWORD         — App-Specific Password from appleid.apple.com
      - name: Check certificate
        id: check_cert
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
        run: |
          echo "=== Secret availability check ==="
          [ -n "$APPLE_CERTIFICATE" ]          && echo "✅ APPLE_CERTIFICATE set"          || echo "❌ APPLE_CERTIFICATE missing"
          [ -n "$APPLE_CERTIFICATE_PASSWORD" ] && echo "✅ APPLE_CERTIFICATE_PASSWORD set"  || echo "❌ APPLE_CERTIFICATE_PASSWORD missing"
          [ -n "$APPLE_TEAM_ID" ]              && echo "✅ APPLE_TEAM_ID set"               || echo "❌ APPLE_TEAM_ID missing"
          [ -n "$APPLE_ID" ]                   && echo "✅ APPLE_ID set"                    || echo "❌ APPLE_ID missing"
          [ -n "$APPLE_APP_PASSWORD" ]         && echo "✅ APPLE_APP_PASSWORD set"          || echo "❌ APPLE_APP_PASSWORD missing"
          echo "================================="
          if [ -n "$APPLE_CERTIFICATE" ]; then
            echo "HAS_CERT=true" >> "$GITHUB_OUTPUT"
          else
            echo "HAS_CERT=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Import certificate
        if: steps.check_cert.outputs.HAS_CERT == 'true'
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        run: |
          KEYCHAIN_PATH="$RUNNER_TEMP/build.keychain"
          KEYCHAIN_PASSWORD="$(openssl rand -hex 16)"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          echo "$APPLE_CERTIFICATE" | base64 --decode -o "$RUNNER_TEMP/cert.p12"
          security import "$RUNNER_TEMP/cert.p12" \
            -k "$KEYCHAIN_PATH" \
            -P "$APPLE_CERTIFICATE_PASSWORD" \
            -T /usr/bin/codesign
          security list-keychain -d user -s "$KEYCHAIN_PATH"
          security set-key-partition-list \
            -S apple-tool:,apple: \
            -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          echo "KEYCHAIN_PATH=$KEYCHAIN_PATH" >> "$GITHUB_ENV"

      # ── Archive ─────────────────────────────────────────────────────────────
      - name: Archive
        env:
          HAS_CERT: ${{ steps.check_cert.outputs.HAS_CERT }}
        run: |
          if [ "$HAS_CERT" = "true" ]; then
            # Signed build with Developer ID + Hardened Runtime (required for notarization)
            xcodebuild archive \
              -scheme CopilotIsland \
              -configuration Release \
              -archivePath "$RUNNER_TEMP/CopilotIsland.xcarchive" \
              "OTHER_CODE_SIGN_FLAGS=--keychain $KEYCHAIN_PATH --timestamp" \
              ENABLE_HARDENED_RUNTIME=YES \
              -quiet
          else
            # Ad-hoc signed build (no Developer ID)
            xcodebuild archive \
              -scheme CopilotIsland \
              -configuration Release \
              -archivePath "$RUNNER_TEMP/CopilotIsland.xcarchive" \
              CODE_SIGN_IDENTITY="-" \
              CODE_SIGNING_REQUIRED=YES \
              CODE_SIGNING_ALLOWED=YES \
              -quiet
          fi

      # ── Re-sign nested binaries (Sparkle XPC services, dylibs) ──────────────
      # Sparkle's pre-built XPC binaries need explicit re-signing with Developer ID
      - name: Re-sign nested binaries
        if: steps.check_cert.outputs.HAS_CERT == 'true'
        run: |
          APP=$(find "$RUNNER_TEMP/CopilotIsland.xcarchive/Products" -name "CopilotIsland.app" | head -1)
          IDENTITY=$(security find-identity -v -p codesigning "$KEYCHAIN_PATH" \
            | grep "Developer ID Application" | head -1 \
            | sed 's/.*"\(.*\)".*/\1/')
          echo "Re-signing with identity: $IDENTITY"

          # Sign dylibs and .so files
          find "$APP/Contents" -type f \( -name "*.dylib" -o -name "*.so" \) | while read f; do
            codesign --force --sign "$IDENTITY" --timestamp "$f" 2>&1 || true
          done

          # Sign XPC services (inside-out: binaries first, then bundle)
          find "$APP/Contents" -type d -name "*.xpc" | while read xpc; do
            codesign --force --sign "$IDENTITY" --timestamp --options runtime "$xpc" 2>&1 || true
          done

          # Sign frameworks (deepest first)
          find "$APP/Contents" -type d -name "*.framework" | sort -rn | while read fw; do
            codesign --force --sign "$IDENTITY" --timestamp --options runtime "$fw" 2>&1 || true
          done

          # Re-sign the whole app bundle
          codesign --force --deep --sign "$IDENTITY" --timestamp --options runtime "$APP"
          echo "✅ Re-signing complete"
          codesign --verify --deep --strict "$APP" && echo "✅ Verification passed"


      - name: Package DMG
        env:
          HAS_CERT: ${{ steps.check_cert.outputs.HAS_CERT }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          APP=$(find "$RUNNER_TEMP/CopilotIsland.xcarchive/Products" -name "CopilotIsland.app" | head -1)
          if [ -z "$APP" ]; then
            echo "❌ Could not locate CopilotIsland.app in archive"; exit 1
          fi

          if [ "$HAS_CERT" != "true" ]; then
            codesign --force --deep --sign - "$APP"
          fi

          DMG_PATH="$RUNNER_TEMP/CopilotIsland-${VERSION}.dmg"
          mkdir -p /tmp/dmg_staging
          cp -R "$APP" /tmp/dmg_staging/
          ln -s /Applications /tmp/dmg_staging/Applications

          hdiutil create \
            -volname "Copilot Island ${VERSION}" \
            -srcfolder /tmp/dmg_staging \
            -ov -format UDZO \
            "$DMG_PATH"

          if [ "$HAS_CERT" = "true" ]; then
            codesign --force --sign "Developer ID Application" --timestamp "$DMG_PATH"
          fi

          echo "DMG_PATH=$DMG_PATH" >> "$GITHUB_ENV"
          echo "DMG_NAME=CopilotIsland-${VERSION}.dmg" >> "$GITHUB_ENV"

      # ── Notarize + Staple (only when Developer ID cert is present) ───────────
      - name: Notarize DMG
        if: steps.check_cert.outputs.HAS_CERT == 'true'
        continue-on-error: true   # failure here still publishes the release (signed but not notarized)
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          SUBMISSION_OUTPUT=$(xcrun notarytool submit "$DMG_PATH" \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_APP_PASSWORD" \
            --team-id "$APPLE_TEAM_ID" \
            --wait \
            --output-format json 2>&1)
          echo "$SUBMISSION_OUTPUT"
          STATUS=$(echo "$SUBMISSION_OUTPUT" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('status','unknown'))" 2>/dev/null || echo "unknown")
          SUBMISSION_ID=$(echo "$SUBMISSION_OUTPUT" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('id',''))" 2>/dev/null || echo "")
          if [ "$STATUS" != "Accepted" ]; then
            echo "❌ Notarization status: $STATUS — fetching Apple rejection log..."
            if [ -n "$SUBMISSION_ID" ]; then
              xcrun notarytool log "$SUBMISSION_ID" \
                --apple-id "$APPLE_ID" \
                --password "$APPLE_APP_PASSWORD" \
                --team-id "$APPLE_TEAM_ID" 2>&1 || true
            fi
            exit 1
          fi
          xcrun stapler staple "$DMG_PATH"

      # ── Create GitHub Release ───────────────────────────────────────────────
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Copilot Island ${{ github.ref_name }}"
          body: |
            ## Copilot Island ${{ github.ref_name }}

            ### Install
            1. Download `${{ env.DMG_NAME }}` below
            2. Open the DMG and drag **Copilot Island.app** to your Applications folder
            3. Launch — the app lives in your MacBook notch ✨

            ### Requirements
            - macOS 14.0+ (Sonoma or later)
            - MacBook with notch (2021 or later)
            - GitHub Copilot CLI installed and authenticated

            ### What's New
            See [CHANGELOG](https://github.com/lordmos/copilot-island/blob/main/CHANGELOG.md) for details.
          files: ${{ env.DMG_PATH }}
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
